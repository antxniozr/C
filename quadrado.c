#include <stdio.h>

int main (){
         int num, resultado;
         for (num = 1; num <= 20; num++) {
             resultado = num * num;
             printf("%d\n", resultado);
         } 
         
         return 0;
}
